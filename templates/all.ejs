<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css"rel="stylesheet"
    integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" 
    crossorigin="anonymous">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <title>Livros</title>
    <style>
        h1 {
            text-align: center;
        }
        .add {
            font-size: 20px;
            color: black;
            text-decoration: none;
            padding: 12px;
            background-color: rgb(201, 201, 201);
            margin: 50px;
        }
        .add:hover {
            background-color: rgb(230, 229, 229);
        }
        .header {
            width: 100%;
            padding: 20px;
            margin: 20px;
            text-align: center;
            background-color: black;
            color: white;
        }

    </style>
</head>
<body>
    <div class="container">
        <div class="header">
        <h1>Estoque Livros </h1>
        </div>
    
    <a class="add" href="/add">Adicionar Livros</a>
        <% for(let i = 0;i < dados.length;i ++) { %>
            <div class="card p-5 m-3" <%=dados[i].id%>>
                <form class="d-flex align-items-center justify-content-between bg-light"  method="POST" action="/?_method=DELETE">
                <div class="d-flex flex-column "> 
                <h1>Book: <%=dados[i].book%></h1>
                <h3>Author: <%=dados[i].author%></h3>
                <h3>Category: <%=dados[i].category%></h3>
                <h3>Amount: <%=dados[i].amount%></h3>
                </div>
                <div class="d-flex flex-column">
                <input name="id" value="<%=dados[i].id%>" hidden>
                <a class="btn btn-info mb-1" href="/edit/<%=dados[i]._id%>">Edit</a> 
                <button class="btn btn-danger" type="submit">Delete</button>
               </div>
            </form>
            </div>
        <% } %>
    </div>    
</body>
</html>